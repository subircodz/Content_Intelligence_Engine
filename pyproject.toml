[project]
name = "power-win-content"
version = "0.1.0"
description = "AI-powered content production system for Power.win"
requires-python = ">=3.13"
dependencies = [
    "httpx",
    "python-docx",
    "python-dotenv",
    "pydantic",
    "playwright",
    "rich",
]
[dependency-groups]
dev = [
    "pytest",
]