[project]
name = "content-intelligence-engine"
version = "0.1.0"
description = "Domain-independent AI content intelligence and production engine"
requires-python = ">=3.13"
dependencies = [
    "httpx",
    "python-docx",
    "python-dotenv",
    "pydantic",
    "playwright",
    "rich",
    "beautifulsoup4",
]
[dependency-groups]
dev = [
    "pytest",
]
